# Handoff — Kater Dev Tools

**Datum:** 2026-06-25  
**Status:** v0.2.0 — gateway robuust, ~85% van plan geïmplementeerd

---

## Wat is gebouwd (sinds v0.1.0)

### Nieuw sinds 2026-06-13

| Feature | Status |
|---------|--------|
| `autofix.py` — `doctor --apply --yes` schrijft écht bestanden | Done |
| `kater chain run` — tool chains uitvoerbaar via CLI | Done |
| `kater config` — render full MCP config per profile | Done |
| `kater adapters` — scan configured external MCP adapters | Done |
| External MCP adapter inventory — detecteert welke externals configured zijn | Done |
| Doctor auto-scans `.cursor/mcp.json` (project + home) | Done |
| Doctor toont adapter config status per source | Done |
| 5 nieuwe native MCP tools: `kater_chains`, `kater_adapters`, `kater_config`, `utrecht_status`, `utrecht_pipeline_status` | Done |
| CI workflow (GitHub Actions ruff + pytest) | Done |
| Smoketest uitgebreid | Done |
| `config/` map aangemaakt voor Docker volume | Done |
| .gitignore, README, docs bijgewerkt | Done |

### CLI Commands (nu 11)

```
kater doctor           Diagnose + autofix
kater profiles         Lijst profielen
kater tools            Lijst native tools per profiel
kater chains           Lijst ketens
kater chain run        Voer keten uit
kater config           Render MCP config per profiel
kater adapters         Scan externe adapters
kater mcp serve        Start MCP server
```

### Native MCP Tools (nu 7)

| Tool | Profile |
|------|---------|
| `kater_profiles` | core |
| `kater_doctor` | core |
| `kater_chains` | core |
| `kater_adapters` | core |
| `kater_config` | core |
| `utrecht_status` | utrecht |
| `utrecht_pipeline_status` | utrecht |

### Tests

48 tests, allemaal groen. ruff check passed.

---

## Plan vs implementatie

| Unit | v0.1.0 (13 jun) | v0.2.0 (25 jun) |
|------|-----------------|-----------------|
| U1 Bootstrap repo | Done | Done |
| U2 Inventory + profiles | Partial | Partial (nog geen live plugin scan) |
| U3 Doctor + autofix | Partial (geen apply) | **Done** |
| U4 Native gateway + chains | Partial (2 tools) | **Done** (7 tools) |
| U5 Docker MCP server | Partial | Done (config map klaar) |
| U6 Deploy docs | Done | Done |
| U7 Utrecht adapter | Partial (config check) | **Done** (status + pipeline) |
| Extern MCP adapter systeem | — | **Done** (inventory + config render) |
| CI | — | **Done** |

---

## Openstaande TODO's voor volgende sessie

| Prioriteit | Taak |
|------------|------|
| Medium | Live Cursor MCP plugin scan (detecteer welke MCPs werkelijk in Cursor geconfigureerd zijn) |
| Medium | Docker compose build/up smoke op host met daemon |
| Low | `kater doctor --apply --yes` detecteren of `.cursor/mcp.json` bestaat en daarin wijzigen |
| Low | Cloudflare deploy optie (uitgesteld) |
